# Core dependencies
Flask>=3.1.0
requests==2.32.5
pathvalidate==3.3.1

# Browser TLS impersonation for GoFile's API edge (ships prebuilt wheels).
# Lets requests to api.gofile.io present a real Chrome TLS fingerprint, which
# gets past the fingerprint-based part of GoFile's bot filtering. Optional:
# the tool falls back to plain `requests` if this is unavailable.
curl_cffi>=0.7.0

# Flask extensions
Flask-WTF>=1.2.1

# Configuration and environment
python-dotenv>=1.0.0
PyYAML>=6.0.1

# System monitoring
psutil>=5.9.8

# Security
certifi==2026.7.22
charset-normalizer==3.5.1
idna==3.11
urllib3==2.6.3

# For testing (uncomment when needed)
# pytest>=7.4.0
# pytest-cov>=4.1.0